## 2024-05-22 - Semantic Navigation
**Learning:** Using `<a>` tags without `href` for page switching creates keyboard traps and semantic confusion. Buttons are better for in-page content switching.
**Action:** Replace fake links with `<button>` elements and manage focus styles explicitly.
